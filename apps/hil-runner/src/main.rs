//! Placeholder for the ph-hil-managed classic ESP32 firmware.

fn main() {
    eprintln!(
        "VEML7700 HIL runner is a scaffold; implement the board and optical fixture adapters before a real build"
    );
    std::process::exit(2);
}

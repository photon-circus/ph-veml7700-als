schema = 1

[shim]
program = "cargo"
args = ["run", "--quiet", "--manifest-path", "tools/ph-hil-build/Cargo.toml", "--"]
working_directory = "."
manifest = "target/ph-hil/veml7700-harness.manifest.toml"

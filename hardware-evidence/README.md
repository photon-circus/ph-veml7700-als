# Hardware evidence

Commit only reviewed references and compact metadata for sealed `ph-hil` runs,
not mutable working directories or vendor documents. Each accepted record should
name the run ID/hash, firmware version, fixture revision, policy hash, instrument
identity/calibration state, and capability decision.

Mock runs never appear as physical support evidence.

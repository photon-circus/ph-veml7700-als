schema = 1

[bench]
name = "veml7700-classic-esp32"
operator = "replace-with-operator"

[metadata.veml7700]
circuit_checks_passed = false
light_source_repeatable = false
optical_fixture_characterized = false
fixture_revision = "replace"
dut_part = "VEML7700-TR-or-TT"
package_orientation = "replace"
sensor_to_diffuser_mm = 0.0
aperture_mm = "replace"
window_material = "replace"
diffuser_material = "replace"
source_type = "replace"
source_spectrum_class = "replace"
source_warmup_s = 0
dark_baseline_recorded = false
reference_calibrated = false
reference_identity = "replace"
reference_calibration_state = "replace"
i2c_pullup_ohms = 4700
logic_voltage_v = 3.3
sensor_vdd_v = 3.3

[roles.dut]
adapter = "serial-dut"
port = "/dev/serial/by-id/replace"
flash = { tool = "espflash", chip = "esp32" }
baud = 115200

[roles.logic]
adapter = "sigrok"
driver = "dreamsourcelab-dslogic"
selector = "replace"
channels = { sda = 0, scl = 1, source = 2 }

# Add source, reference meter, scope, and switchable supply roles according to
# the installed ph-hil adapters before enabling calibrated optical work.
